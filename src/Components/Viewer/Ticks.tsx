import React from 'react';
import styled from '@emotion/styled';

const StyledRoot = styled.div`
    
`

type Props = {

}

export const Ticks: React.FC<Props> = ({ }) => {


    return (
        <StyledRoot>
            t
        </StyledRoot>
    )
}